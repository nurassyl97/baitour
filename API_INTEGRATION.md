# 🔌 API Integration Guide for BaiTour

Этот документ описывает, как интегрировать API с вашим проектом BaiTour.

## 📋 Содержание

1. [Текущее состояние](#текущее-состояние)
2. [Требования к API](#требования-к-api)
3. [Быстрый старт](#быстрый-старт)
4. [Структура API](#структура-api)
5. [Переключение на реальный API](#переключение-на-реальный-api)
6. [Тестирование](#тестирование)

---

## 🎯 Текущее состояние

**Сейчас сайт использует mock данные** из файла `data/tours.json`.

Подготовлена инфраструктура для переключения на реальный API:
- ✅ Создан API service layer (`lib/api.ts`)
- ✅ Настроены environment variables
- ✅ Добавлены компоненты загрузки и ошибок
- ✅ Реализовано автоматическое fallback на mock данные

---

## 📝 Требования к API

### Базовые требования

Ваш API должен предоставлять следующие endpoints:

#### 1. **GET /tours** - Получить все туры
```json
Response:
{
  "tours": [
    {
      "id": "1",
      "name": "Название тура",
      "slug": "nazvanie-tura",
      "country": "Страна",
      "city": "Город",
      "duration": "5 дней / 4 ночи",
      "price": 599,
      "currency": "USD",
      "rating": 4.8,
      "reviewCount": 234,
      "image": "https://...",
      "images": ["https://...", "https://..."],
      "description": "Описание тура...",
      "highlights": ["Пункт 1", "Пункт 2"],
      "included": ["Включено 1", "Включено 2"],
      "excluded": ["Не включено 1"],
      "hotel": {
        "name": "Название отеля",
        "rating": 4.5,
        "amenities": ["WiFi", "Бассейн"]
      },
      "maxGuests": 12,
      "minGuests": 2
    }
  ]
}
```

#### 2. **GET /tours/:id** - Получить тур по ID
```json
Response: (single tour object)
{
  "id": "1",
  "name": "...",
  ...
}
```

#### 3. **GET /tours/search** - Поиск туров
```
Query Parameters:
- country: string (optional)
- city: string (optional)
- minPrice: number (optional)
- maxPrice: number (optional)
- sortBy: 'price-asc' | 'price-desc' | 'rating' | 'duration' (optional)

Response: same as GET /tours
```

#### 4. **POST /bookings** - Отправить бронирование
```json
Request:
{
  "tourId": "1",
  "fullName": "Иван Иванов",
  "email": "ivan@example.com",
  "phone": "+7 700 123 4567",
  "travelDate": "2026-03-15",
  "adults": 2,
  "children": 0,
  "specialRequests": "Дополнительные пожелания"
}

Response:
{
  "bookingId": "BOOKING-123456",
  "message": "Бронирование успешно создано"
}
```

### Опциональные endpoints

- **GET /tours/slug/:slug** - Получить тур по slug
- **GET /tours/countries** - Получить список стран
- **GET /tours/cities?country=...** - Получить города для страны

---

## 🚀 Быстрый старт

### Шаг 1: Получите данные API

Когда получите API, вам нужно узнать:
1. **Base URL** (например, `https://api.baitour.com`)
2. **API Key** (если требуется аутентификация)
3. **Документацию по endpoints**

### Шаг 2: Настройте environment variables

Откройте файл `.env.local` и заполните:

```bash
# Базовый URL вашего API
NEXT_PUBLIC_API_BASE_URL=https://api.baitour.com

# API ключ (если требуется)
NEXT_PUBLIC_API_KEY=your_actual_api_key_here

# Переключите на 'api' для использования реального API
NEXT_PUBLIC_DATA_SOURCE=api
```

### Шаг 3: Перезапустите сервер

```bash
npm run dev
```

**Готово!** Сайт теперь использует реальный API.

---

## 🔧 Структура API Service

Файл `lib/api.ts` содержит все функции для работы с API:

```typescript
import * as api from '@/lib/api';

// Получить все туры
const tours = await api.getAllTours();

// Получить тур по ID
const tour = await api.getTourById('1');

// Поиск туров
const results = await api.searchTours({
  country: 'Турция',
  city: 'Стамбул',
  minPrice: 400,
  maxPrice: 800,
  sortBy: 'price-asc'
});

// Отправить бронирование
const response = await api.submitBooking({
  tourId: '1',
  fullName: 'Иван Иванов',
  email: 'ivan@example.com',
  phone: '+7 700 123 4567',
  travelDate: '2026-03-15',
  adults: 2,
  children: 0,
  specialRequests: 'Веганское питание'
});
```

---

## 🔄 Переключение на реальный API

### Вариант 1: Через .env.local (Рекомендуется)

```bash
# В файле .env.local
NEXT_PUBLIC_DATA_SOURCE=api
```

### Вариант 2: Поэтапное тестирование

Вы можете тестировать API постепенно:

1. **Тестируем только чтение туров:**
```bash
NEXT_PUBLIC_DATA_SOURCE=api
```

2. **Если API не работает**, код автоматически вернется к mock данным (fallback)

3. **Логи в консоли** покажут успешность запросов

---

## 🧪 Тестирование

### Проверьте работу API

1. **Откройте консоль браузера** (F12)
2. **Перейдите на главную страницу**
3. **Смотрите логи:**
   - ✅ Успешный запрос: данные загружаются с API
   - ❌ Ошибка: автоматически используются mock данные

### Тестовые сценарии

- [ ] Главная страница загружается
- [ ] Поиск туров работает
- [ ] Детальная страница тура открывается
- [ ] Форма бронирования отправляется
- [ ] Обработка ошибок работает корректно

---

## ⚙️ Настройка API под ваш формат

Если ваш API возвращает данные в другом формате, вам нужно изменить файл `lib/api.ts`:

### Пример: Разная структура ответа

Если ваш API возвращает:
```json
{
  "data": { "items": [...] },
  "status": "success"
}
```

Измените в `lib/api.ts`:
```typescript
export async function getAllTours(): Promise<Tour[]> {
  // ...
  const response = await apiFetch<{ data: { items: Tour[] } }>('/tours');
  return response.data.items; // Вместо response.tours
}
```

### Пример: Другие названия полей

Если API использует другие названия полей (например, `title` вместо `name`), создайте функцию преобразования:

```typescript
function transformTourFromAPI(apiTour: any): Tour {
  return {
    id: apiTour.id,
    name: apiTour.title, // Преобразование
    slug: apiTour.slug,
    country: apiTour.destination.country,
    city: apiTour.destination.city,
    // ... остальные поля
  };
}
```

---

## 🔐 Безопасность

### API ключи

- ✅ Никогда не коммитьте `.env.local` в git
- ✅ `.env.local` уже в `.gitignore`
- ✅ Используйте `.env.local.example` для документации

### CORS

Если возникают CORS ошибки:
1. Убедитесь, что ваш API разрешает запросы с домена сайта
2. В Next.js можно настроить proxy в `next.config.ts`

---

## 📞 Поддержка

Если у вас возникли проблемы:

1. Проверьте логи в консоли браузера
2. Проверьте логи сервера разработки
3. Убедитесь, что `.env.local` настроен правильно
4. Проверьте формат ответов API (должен совпадать с типами в `lib/data.ts`)

---

## ✅ Чек-лист перед запуском

- [ ] Получен Base URL API
- [ ] Получен API Key (если нужен)
- [ ] Протестированы endpoints в Postman/Insomnia
- [ ] Заполнен `.env.local`
- [ ] Сервер перезапущен
- [ ] Проверена работа на localhost
- [ ] Тесты пройдены успешно
- [ ] Готово к деплою на Vercel

---

## 🎉 Готово!

После настройки API ваш сайт будет:
- ✅ Загружать реальные туры из базы данных
- ✅ Обрабатывать бронирования
- ✅ Автоматически возвращаться к mock данным при ошибках
- ✅ Показывать loading индикаторы
- ✅ Обрабатывать ошибки корректно
