# ✅ API Migration Checklist - Чек-лист для интеграции API

## 📋 Что нужно от вас завтра

Когда получите доступ к API, вам понадобится:

### 1. Информация об API
- [ ] **Base URL** - базовый адрес API (например: `https://api.baitour.com`)
- [ ] **API Key / Token** - ключ для аутентификации (если требуется)
- [ ] **Документация** - описание endpoints и форматов данных
- [ ] **Примеры ответов** - чтобы проверить структуру данных

### 2. Endpoints, которые нам нужны

#### Обязательные:
- [ ] `GET /tours` - список всех туров
- [ ] `GET /tours/:id` - информация о конкретном туре
- [ ] `POST /bookings` - создание бронирования

#### Желательные:
- [ ] `GET /tours/search` - поиск туров с фильтрами
- [ ] `GET /tours/countries` - список стран
- [ ] `GET /tours/cities` - список городов

---

## 🚀 Шаги по интеграции (на завтра)

### Шаг 1: Получите API credentials (5 мин)
```bash
# Запишите:
Base URL: ___________________________
API Key:  ___________________________
```

### Шаг 2: Обновите .env.local (2 мин)
Откройте файл `/Users/nurassyllk/tours/.env.local` и замените:

```bash
# Замените эти строки:
NEXT_PUBLIC_API_BASE_URL=https://ваш-api-url.com
NEXT_PUBLIC_API_KEY=ваш-ключ-api

# Переключите на реальный API:
NEXT_PUBLIC_DATA_SOURCE=api
```

### Шаг 3: Перезапустите сервер (1 мин)
```bash
# Остановите текущий сервер (Ctrl+C)
# Запустите заново:
npm run dev
```

### Шаг 4: Проверьте работу (10 мин)
- [ ] Откройте http://localhost:3000
- [ ] Проверьте главную страницу - туры загружаются
- [ ] Проверьте поиск - фильтры работают
- [ ] Откройте страницу тура - детали отображаются
- [ ] Попробуйте забронировать - форма отправляется

### Шаг 5: Проверьте консоль браузера (F12)
Смотрите на логи:
- ✅ Нет красных ошибок → всё работает
- ❌ Есть ошибки → смотрите раздел "Troubleshooting" ниже

---

## 🔧 Возможные проблемы и решения

### Проблема 1: CORS ошибки
```
Access to fetch has been blocked by CORS policy
```

**Решение:** Попросите backend разработчика добавить ваш домен в CORS whitelist:
- `http://localhost:3000`
- `http://localhost:3001`
- Ваш production домен (например, `https://baitour.com`)

### Проблема 2: Неправильный формат данных
```
TypeError: Cannot read property 'tours' of undefined
```

**Решение:** API возвращает данные в другом формате. Нужно изменить `lib/api.ts`:

1. Откройте консоль браузера (F12)
2. Посмотрите, какой формат приходит от API
3. Изменить код в соответствии с форматом (см. `API_INTEGRATION.md`)

### Проблема 3: 401 Unauthorized
```
API Error: 401 Unauthorized
```

**Решение:** Неправильный API ключ или неправильный формат headers.

Проверьте в `lib/api.ts` строку:
```typescript
headers['Authorization'] = `Bearer ${API_KEY}`;
```

Возможно нужно изменить на:
```typescript
headers['X-API-Key'] = API_KEY;
// или
headers['api-key'] = API_KEY;
```

### Проблема 4: Сайт показывает старые данные
**Решение:** Очистите кэш браузера (Cmd+Shift+R)

---

## 📝 Структура данных тура

Ваш API должен возвращать туры в таком формате (или похожем):

```json
{
  "id": "1",
  "name": "Название тура",
  "slug": "nazvanie-tura",
  "country": "Турция",
  "city": "Стамбул",
  "duration": "5 дней / 4 ночи",
  "price": 599,
  "currency": "USD",
  "rating": 4.8,
  "reviewCount": 234,
  "image": "https://example.com/image.jpg",
  "images": ["url1", "url2", "url3"],
  "description": "Полное описание тура...",
  "highlights": [
    "Экскурсия 1",
    "Экскурсия 2"
  ],
  "included": [
    "Что включено 1",
    "Что включено 2"
  ],
  "excluded": [
    "Что не включено 1"
  ],
  "hotel": {
    "name": "Название отеля",
    "rating": 4.5,
    "amenities": ["WiFi", "Бассейн", "Спа"]
  },
  "maxGuests": 12,
  "minGuests": 2
}
```

Если названия полей отличаются - нужно будет создать mapping (см. `API_INTEGRATION.md`)

---

## 🎯 Что уже готово

✅ **API Service Layer** - все функции для работы с API уже написаны
✅ **Environment Variables** - файлы `.env` созданы
✅ **Fallback на Mock данные** - если API не работает, сайт использует тестовые данные
✅ **Loading индикаторы** - компоненты для отображения загрузки
✅ **Error handling** - обработка ошибок
✅ **TypeScript типы** - все типы данных описаны

---

## 📞 Контакты для вопросов

Если что-то не работает:

1. **Проверьте логи в консоли браузера** (F12 → Console)
2. **Проверьте логи сервера** (терминал где запущен `npm run dev`)
3. **Проверьте файл** `API_INTEGRATION.md` для детальной информации
4. **Проверьте формат ответов API** - возможно нужна адаптация

---

## ⏱️ Примерное время интеграции

- ✅ Базовая настройка: **5-10 минут**
- ✅ Если формат данных совпадает: **готово сразу**
- ⚠️ Если нужна адаптация формата: **30-60 минут**
- ⚠️ Если есть сложности с CORS/Auth: **1-2 часа**

---

## 🎉 После успешной интеграции

- [ ] Протестируйте все страницы
- [ ] Проверьте бронирование
- [ ] Задеплойте на Vercel (с новыми env variables)
- [ ] Проверьте работу на production

**Удачи! 🚀**
